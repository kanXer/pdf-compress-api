services:
  - type: web
    name: pdf-compress-api
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y ghostscript poppler-utils
      npm install
    startCommand: node server.js
